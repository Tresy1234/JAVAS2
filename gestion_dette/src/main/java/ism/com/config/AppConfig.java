package ism.com.config;

public class AppConfig {
    // Définit si on utilise un repository en mémoire (list) ou en base de données (bd)
    public static final String REPOSITORY_TYPE = "bd"; // Changez en "list" pour tester avec des listes en mémoire
}
